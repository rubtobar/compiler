const int TIMES = 10;

main {
    write('Introdueix un numero:');
    int loopCount = TIMES;
    TIMES = 3;
    int counter = 0;
    boolean n = readInt();
    whle (loopCount > 0){
        write(n + counter);
        loopCount = loopCount - 1;
        counter = counter + 1;
    }
}